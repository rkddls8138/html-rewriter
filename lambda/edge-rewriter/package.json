{
  "name": "html-rewriter-edge",
  "version": "1.0.0",
  "description": "Lambda@Edge function for HTML rewriting",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outdir=dist --minify",
    "package": "npm run build && cd dist && zip -r ../edge-rewriter.zip .",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  }
}
